OPENAI_API_KEY=<key goes here>

# Optional: Customize the OpenAI models used for translation
# OPENAI_TRANSLATION_MODEL=gpt-5-mini
# OPENAI_TIEBREAKER_MODEL=gpt-5.2

# Optional: Set the default tiebreak mode
# Valid values: tiebreak, choose_existing, choose_new
# - tiebreak: Use AI to determine which translation is better (default)
# - choose_existing: Always keep the existing translation
# - choose_new: Always use the new translation
# TIEBREAK_MODE=tiebreak

# Optional: Rate limiting configuration
# Maximum number of concurrent API requests (default: 10)
# MAX_CONCURRENT_REQUESTS=10

# Initial delay in seconds before retrying after rate limit (default: 1.0)
# INITIAL_RETRY_DELAY=1.0

# Maximum backoff resets before giving up (default: 5, ~10 minutes total)
# Backoff pattern: 1 -> 2 -> 4 -> 8 -> 16 -> 32 -> 64 -> 16 -> 32 -> 64 -> ...
# Each reset cycle (16+32+64 = 112s) counts as one reset
# MAX_BACKOFF_RESETS=5

# Number of tiebreaker API calls to run in parallel per language (default: 10)
# When translations differ from existing, tiebreakers decide which to keep.
# Higher values speed up processing but may increase rate limiting.
# TIEBREAKER_BATCH_SIZE=10
